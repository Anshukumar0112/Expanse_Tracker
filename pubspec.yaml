name: personal_expanse
description: A new Flutter project.

# 👇 Add this environment section exactly like this
environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.0.5

  # Local storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2

  # Notifications and scheduling
  flutter_local_notifications: ^17.1.2
  timezone: ^0.9.3

  # Utility
  intl: ^0.19.0
  uuid: ^4.2.2

  # UI & UX
  google_fonts: ^6.2.1
  url_launcher: ^6.3.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.9
  flutter_lints: ^4.0.0
